name: Summarize new issues

on:
  issues:
    types: [opened]

jobs:
  summary:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sanitize issue title and body
        id: sanitize
        run: |
          echo "title=$(echo "${{ github.event.issue.title }}" | jq -Rs .)" >> $GITHUB_OUTPUT
          echo "body=$(echo "${{ github.event.issue.body }}" | jq -Rs .)" >> $GITHUB_OUTPUT

      - name: Run AI inference
        id: inference
        uses: actions/ai-inference@v1
        with:
          prompt: |
            Summarize the following GitHub issue in one paragraph:
            Title: ${{ fromJson(steps.sanitize.outputs.title) }}
            Body: ${{ fromJson(steps.sanitize.outputs.body) }}

      - name: Validate AI response
        run: |
          echo "${{ steps.inference.outputs.response }}" > /tmp/ai_response_raw.txt
          # Check for potentially unsafe content (HTML/script tags, iframes, etc.)
          if grep -Ei '<script|<iframe|<img|<a |javascript:' /tmp/ai_response_raw.txt; then
            echo "Unsafe content detected in AI response. Failing workflow."
            exit 1
          fi

      - name: Comment with AI summary
        run: |
          # Sanitize AI response before posting
          cat /tmp/ai_response_raw.txt | jq -Rs . | jq -r . > /tmp/ai_summary.txt
          gh issue comment $ISSUE_NUMBER --body-file /tmp/ai_summary.txt
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
